$(function() {

  //$('#leftNav > li:not(.active-trail) > ol').hide()

  //$('#leftNav > li:not(.active-trail)').removeClass('expanded');

  //$('#leftNav > li > ol a.active').each(function() {

  //  var node = $(this);

  //  while( node.length > 0 ) {

  //    var li = node.closest('li');

  //    if( li.length == 0 )
  //      return;

      // We need all the previous, and all of the next, except the immediately next.

      //var sibsWithChildren = li.prevAll().add(li.next().nextAll()).has('ul');
  //    var sibsWithChildren = li.siblings().has('ul');

  //    sibsWithChildren.hide();



  //    node = li.parent();

  //  }

  //});

});
